# Jetpack Environment Configuration
# Copy this file to .env and customize for your setup

# =============================================================================
# RUNTIME MODE
# =============================================================================
# Controls how Jetpack operates:
#   - "local" (default): Full local orchestrator with agents (standard mode)
#   - "hybrid": Uses Cloudflare Worker API for task/memory storage
#   - "cloud": Full cloud mode (future)
JETPACK_MODE=local

# =============================================================================
# CLOUDFLARE WORKER API (required for hybrid/cloud modes)
# =============================================================================
# URL of the deployed Jetpack Worker API
# Deploy from packages/worker-api to your Cloudflare account
# CLOUDFLARE_API_URL=https://your-worker.your-subdomain.workers.dev

# API token for authenticating with the Worker API
# Generate this in your Cloudflare dashboard or use wrangler secret
# CLOUDFLARE_API_TOKEN=your-secret-token

# =============================================================================
# LLM PROVIDER API KEYS
# =============================================================================
# Anthropic API key for Claude supervisor
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API key (alternative to Claude)
# OPENAI_API_KEY=sk-...

# Ollama base URL (for local LLM, no API key needed)
# OLLAMA_BASE_URL=http://localhost:11434

# =============================================================================
# WORKING DIRECTORY
# =============================================================================
# Override the working directory for Jetpack data
# This is where .beads/, .cass/, and .jetpack/ directories are created
# JETPACK_WORK_DIR=/path/to/your/project

# =============================================================================
# WEB UI
# =============================================================================
# Port for the web UI (default: 3002)
# PORT=3002
